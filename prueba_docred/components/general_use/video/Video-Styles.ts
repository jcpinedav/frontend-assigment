import { createUseStyles } from "react-jss";

export const videoStyles = createUseStyles({
  mainContainerVideo: {
    marginTop: "100px",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
});
